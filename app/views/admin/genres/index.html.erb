<h1>ジャンル一覧・追加</h1>

<!--form?with ジャンル登録部分-->

<%= form_with model: @genre, url:'/admin/genres', local:true do |f| %>

  <label>ジャンル名</label>
  <%= f.text_field :name %>
  
  <%= f.submit '新規投稿' %>

<% end %>